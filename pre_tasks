- name: Playbook Windows com Kerberos
  hosts: campogrande3
  gather_facts: no

  pre_tasks:
    - name: Executar kinit com senha
      ansible.builtin.shell: |
        echo "{{ kerberos_password }}" | kinit {{ kerberos_principal }}
      no_log: true

  tasks:
    - name: Teste de ping no Windows
      ansible.windows.win_ping:
